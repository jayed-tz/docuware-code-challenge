# Docuware Frontend for the code challenge

This project uses a backend API to facilitate data display and data insertion.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. 

